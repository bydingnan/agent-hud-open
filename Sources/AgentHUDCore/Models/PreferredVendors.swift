import Foundation

/// Sources enabled by the personal one-tap preset and on a fresh install.
public enum PreferredVendors {
    public static let personal: Set<String> = [
        "Kimi", "GLM", "Cursor", "OpenCode", "OpenCode Go", "Codex", "ZenMux", "Grok",
    ]
}
